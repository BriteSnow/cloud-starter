# By Default ignore all .* (can make exception)
.*

# Include .gitignore
!.gitignore

# Include .vscode (for debugging)
!.vscode/

# ignore node things
node_modules/
npm-debug.log

# Ignore gke-prod (for prod)
k8s/gke-prod

# Ignore package-lock.json 
# (this is debatable, but package-lock.json could pause more issues than anything else in certain scenarios)
package-lock.json

# rollup typescript error
.rpt2_cache/

# prevent compiled css and js files 
# Note: typically, we have a src/lib-bundle.js as part of the source, but it will be included as it does not below.
js/*.js
css/*.css
test/*.js
test/*.css

# ignore the dev-test.ts files, they are for personal development
test-dev.ts

# prevent the .map
*.map

# We ignore all dist folder
dist/