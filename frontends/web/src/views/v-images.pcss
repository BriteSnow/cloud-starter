v-images{
	section.content{
		padding: 1rem;
		
		display: grid;
		grid-template-columns: repeat(auto-fill, 16rem);
		grid-template-rows: repeat(auto-fill, 12rem);
		grid-gap: 2rem;

		

		.card{

			section > video {
				width: 100%;
				
			}
		}
	}


}