apiVersion: v1
kind: Service
metadata:
  labels:
    run: cba-web-server
  name: cba-web-server-srv
  namespace: default
spec:
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    run: cba-web-server
  type: LoadBalancer